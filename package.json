{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.14.1"
  },
  "scripts": {
    "dev": "lerna run dev --parallel",
    "format": "lerna run format --parallel",
    "lint": "lerna run lint --parallel"
  }
}
